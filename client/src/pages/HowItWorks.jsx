import ComingSoon from '../components/common/ComingSoon.jsx';

export default function HowItWorks() {
  return (
    <ComingSoon
      eyebrow="Support"
      title="How it works"
      description="Estimate → small deposit to book → we do the work → we send an invoice with the final price → you pay the balance. A full walkthrough is on the way."
      phase="Phase 4"
    />
  );
}
